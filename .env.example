# Example .env for Expense Tracker
# Copy this file to .env and fill in real values (do NOT commit .env to git).

# PostgreSQL connection string (Neon or other Postgres)
# Format: postgres://USER:PASSWORD@HOST:PORT/DBNAME
# Example (replace with your real DB credentials):
DATABASE_URL="postgres://db_user:supersecretpassword@ep-example-12345.eu-central-1.neon.tech:5432/expense_db"

# Django secret key (production only) - generate a strong random string
SECRET_KEY="replace-with-a-long-random-secret"

# DEBUG: 1 = on (dev), 0 = off (production)
DEBUG=1

# Optional: override allowed hosts (comma-separated) in some deployments
# ALLOWED_HOSTS=127.0.0.1,localhost,your-domain.com

# Optional: email settings (if you want to enable sending email)
# EMAIL_HOST=smtp.example.com
# EMAIL_PORT=587
# EMAIL_HOST_USER=your@email.com
# EMAIL_HOST_PASSWORD=your-email-password
# EMAIL_USE_TLS=1

# Notes:
# - The project is configured to read .env from BASE_DIR/.env via python-dotenv if present.
# - Keep the real .env file private and never commit it to version control.
# - If DATABASE_URL is not provided, the project falls back to SQLite (db.sqlite3) for local testing.
